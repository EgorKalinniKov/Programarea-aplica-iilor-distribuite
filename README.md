# 🏦 Приложение учёта домашних расходов - Микросервисная архитектура

Веб-приложение для учёта домашних расходов, построенное на **микросервисной архитектуре** с использованием **ASP.NET Core 8.0** и **Razor Pages**.

---

## 🏗️ Архитектура микросервисов

Приложение состоит из следующих микросервисов:

### **1. API Gateway** (порт 8000)
- **Технология**: ASP.NET Core + Yarp Reverse Proxy
- **Функции**: 
  - Единая точка входа для всех клиентов
  - Маршрутизация запросов к микросервисам
  - Агрегация API

### **2. Expense Service** (порт 8001)
- **Технология**: ASP.NET Core Web API + Entity Framework Core + PostgreSQL
- **База данных**: `postgres-expenses:5432`
- **Функции**:
  - Управление расходами (CRUD операции)
  - Хранение данных о расходах
  - REST API для работы с расходами

### **3. Category Service** (порт 8002)
- **Технология**: ASP.NET Core Web API + Entity Framework Core + PostgreSQL
- **База данных**: `postgres-categories:5432`
- **Функции**:
  - Управление категориями расходов
  - Предустановленные категории (Продукты, Транспорт, и т.д.)
  - REST API для работы с категориями

### **4. Statistics Service** (порт 8003)
- **Технология**: ASP.NET Core Web API (stateless)
- **Функции**:
  - Расчёт статистики по расходам
  - Дневная, месячная статистика
  - Статистика по категориям
  - Общая сводка (сумма, среднее, мин/макс)

### **5. Import/Export Service** (порт 8004)
- **Технология**: ASP.NET Core Web API (stateless)
- **Функции**:
  - Импорт расходов из CSV
  - Экспорт расходов в CSV
  - Генерация шаблонов CSV

### **6. Web App** (порт 5000)
- **Технология**: ASP.NET Core Razor Pages + Bootstrap 5
- **Функции**:
  - Веб-интерфейс пользователя
  - Управление расходами и категориями
  - Просмотр статистики
  - Импорт/экспорт данных

---

## 🗂️ Структура проекта

```
lab2/
├── src/
│   ├── ApiGateway/              # API Gateway (Yarp)
│   │   ├── Controllers/
│   │   ├── Program.cs
│   │   ├── appsettings.json
│   │   ├── ApiGateway.csproj
│   │   └── Dockerfile
│   │
│   ├── ExpenseService/          # Сервис расходов
│   │   ├── Controllers/
│   │   ├── Data/
│   │   ├── Models/
│   │   ├── Program.cs
│   │   ├── ExpenseService.csproj
│   │   └── Dockerfile
│   │
│   ├── CategoryService/         # Сервис категорий
│   │   ├── Controllers/
│   │   ├── Data/
│   │   ├── Models/
│   │   ├── Program.cs
│   │   ├── CategoryService.csproj
│   │   └── Dockerfile
│   │
│   ├── StatisticsService/       # Сервис статистики
│   │   ├── Controllers/
│   │   ├── Models/
│   │   ├── Program.cs
│   │   ├── StatisticsService.csproj
│   │   └── Dockerfile
│   │
│   ├── ImportExportService/     # Сервис импорта/экспорта
│   │   ├── Controllers/
│   │   ├── Models/
│   │   ├── Program.cs
│   │   ├── ImportExportService.csproj
│   │   └── Dockerfile
│   │
│   └── WebApp/                  # Веб-приложение (Razor Pages)
│       ├── Pages/
│       ├── wwwroot/
│       ├── Program.cs
│       ├── WebApp.csproj
│       └── Dockerfile
│
├── docker-compose.yml           # Оркестрация всех сервисов
└── README.md                    # Этот файл
```

---

## 🚀 Запуск приложения

### Предварительные требования:
- Docker Desktop
- Docker Compose

### Команды запуска:

```bash
# Перейти в папку проекта
cd C:\WorkDir\Labs\PAD\lab2

# Запустить все микросервисы
docker-compose up --build

# Запустить в фоновом режиме
docker-compose up -d --build

# Остановить все сервисы
docker-compose down

# Остановить и удалить volumes (БД)
docker-compose down -v
```

---

## 🌐 Доступ к сервисам

После запуска приложение будет доступно по следующим адресам:

| Сервис | URL | Описание |
|--------|-----|----------|
| **Web App** | http://localhost:5000 | Веб-интерфейс приложения |
| **API Gateway** | http://localhost:8000 | API Gateway |
| **Expense Service** | http://localhost:8001 | API расходов |
| **Category Service** | http://localhost:8002 | API категорий |
| **Statistics Service** | http://localhost:8003 | API статистики |
| **Import/Export Service** | http://localhost:8004 | API импорта/экспорта |

---

## 📊 Технологический стек

- **Backend**: ASP.NET Core 8.0
- **Frontend**: Razor Pages + Bootstrap 5 + JavaScript
- **ORM**: Entity Framework Core 8.0
- **База данных**: PostgreSQL 15
- **API Gateway**: Yarp Reverse Proxy 2.1
- **Контейнеризация**: Docker + Docker Compose
- **Архитектура**: Microservices

---

## ✨ Функциональность

- ✅ Добавление, редактирование, удаление расходов
- ✅ Управление категориями расходов
- ✅ Статистика по расходам (дневная, месячная, по категориям)
- ✅ Импорт расходов из CSV
- ✅ Экспорт расходов в CSV
- ✅ Адаптивный веб-интерфейс
- ✅ Микросервисная архитектура
- ✅ Database per Service pattern
- ✅ Health checks для всех сервисов

---

## 🔧 Разработка

Каждый микросервис можно запускать отдельно для разработки:

```bash
cd src/ExpenseService
dotnet run
```

Swagger UI доступен для каждого сервиса в режиме разработки:
- http://localhost:8001/swagger (Expense Service)
- http://localhost:8002/swagger (Category Service)
- http://localhost:8003/swagger (Statistics Service)
- http://localhost:8004/swagger (Import/Export Service)

